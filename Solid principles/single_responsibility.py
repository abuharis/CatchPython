class Student():
    def __init__(self, name, id, email):
        self.name = name
        self.id = id
        self.email = email

    def save_to_db(self):


    def enroll(self):


'''Make sure to add only related features into the class giving single responsibility
Other developer can reuse the same code or extend the functionality without changing the file'''